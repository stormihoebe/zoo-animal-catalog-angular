import {Component, Input, Output, EventEmitter} from '@angular/core';
import  {Animal} from './animal.model';

@Component({
  selector: 'animal-edit',
  template: ` <h1>Animal Edit Here</h1>

  `

})

export class AnimalEditComponent{

}
